# Site images

Drop these 4 photos into this folder with these exact filenames:

- `before-1.jpg` — overgrown bed with compost bin (your first "BEFORE")
- `after-1.jpg`  — cleared bed with lattice and blue ball (your first "AFTER")
- `before-2.jpg` — overgrown tree base with weeds (your second "BEFORE")
- `after-2.jpg`  — cleared tree base with brick edging and gravel (your second "AFTER")

Optional hero photo:
- `hero.jpg` — a single bright "finished garden" photo for the top of the page

No resizing needed — the CSS handles it. JPG or PNG both work.
